@model IEnumerable<RepositoryPattern.Model.Catalog.ProductCategory>
@{var csspart = "~/Content/themes/Plumb";
  var imgpath = "~/Images/WallImages/imagepath";
  }
  <br> 
  <div class="container" id="intro">
  	<div class="row">
  		<div class="col-lg-4"><img  src="@Url.Content(imgpath + "/image002.jpg")" /></div>
  		<div class="col-lg-4"><img  src="@Url.Content(imgpath + "/image003.jpg")" /></div>
  		<div class="col-lg-4"><img  src="@Url.Content(imgpath + "/image004.jpg")" /></div>
    </div>
   <br>
   <br>
   <p>Stop running around searching for parts and get what you need fast. Ninety-Five percent of orders are shipped the same day, and usually arrive the next day. Bob Garrecht Supply carries a vast array of plumbing parts. Whether you need parts for a fairly new toilet or a valve for a 1950's faucet. We probably have it. If we we don't, we can get it for you. Call, email or place your order right here on our website.</p>
  </div>

<div class="main-content">
<div class="container">
  <div class="page-header">
  <h1><small>Feature Products</small></h1>
  </div>
@if(Model != null)
{
    //var ss = Model.Select(x => x.Category.Name).FirstOrDefault();
<div class="col-md-4">
    <div class="thumbnail">
      <img src="@Url.Content(csspart + "/img/featured-products.jpg")" alt="...">
      <div class="caption">
        <h3>Feature Produts</h3>
        <p>Cras justo odio, dapibus ac facilisis in, egestas eget quam. Donec id elit non mi porta gravida at eget metus. Nullam id dolor id nibh ultricies vehicula ut id elit.</p>
      </div>
    </div>
  </div>
    
  
<div class="col-md-8">
    @foreach(var cat in Model)
    {
        int i = cat.Product.ID;
    <div class="col-sm-3">
         @{ if(cat.Picture!=null)
         {  
           <a href="@Url.Action("Product", "Home", new {prd=cat.Product.ID, catID =cat.CategoryID??default(int)})"><img class="small-thumbnail" style="height:auto; width:auto; max-width:170px; max-height:200px;"  src="@Url.Content(imgpath + "/" + cat.Picture.FilePath)"></a>
         }
         else{<a href="#"><img class="small-thumbnail" style="height:auto; width:auto; max-width:170px; max-height:200px;" src="@Url.Content(csspart + "/img/I08000.jpg")"></a>}
         }
    <a><label class="plabel">@cat.Product.Name</label></a>
    </div>
    
    }
</div>

}
</div>
</div>