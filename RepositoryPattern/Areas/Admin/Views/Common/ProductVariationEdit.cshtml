@model RepositoryPattern.Model.Catalog.ProductVariation 
<div role="form">
        <div class="box-body">
            <div class="form-group">
                @Html.Label("Variation Description", new {@for="" })
                @Html.TextBox("description", Model.Description, new {@class="form-control",placeholder="Name" })
                @Html.ValidationMessage("Variation")
            </div>
                <div class="form-group">
                @Html.Label("size",null, new {@for="" })
                @Html.TextBox("size",Model.Size, new {@class="form-control" })
                @Html.ValidationMessage("ShortDescription")
            </div>
                                       
                </div>
     </div>